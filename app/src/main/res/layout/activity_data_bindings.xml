<?xml version="1.0" encoding="utf-8"?>
<layout xmlns:android="http://schemas.android.com/apk/res/android">
    <data>
        <import type="android.graphics.Color"/>
        <variable name="user" type="com.zuzya.chat.databindings.User"/>
    </data>
    <LinearLayout
        android:orientation="vertical"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:paddingTop="40dp"
       >
        <TextView android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:paddingLeft="@{user.paddingLeft}"

            android:text='@{"FirstName: " + user.firstName + "; LeftPadding: " + user.paddingLeft}'/>
        <TextView android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:paddingLeft="@{(user.paddingLeft / 3 ) * 4}"
            android:text='@{"LastName: " + user.lastName + "; LeftPadding x 1.3: " + user.paddingLeft}'/>
    </LinearLayout>
</layout>